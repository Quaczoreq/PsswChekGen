# PsswChekGen
password cgenerator, password strenght compatibility check, crack time for given password.

This is a smal project inspired by Scott Rayner, He has created the pythone code for the password strenght calculator.
Rest of the project is a palyful code combination of HTML, CSS, and javascript.

## How to use
Clone the repo or copy `password_checker.py` into your local python environment and run it.

## HTML version
Open `index.html` in your browser to use the same checker as a web page.

If you want to serve it locally instead of opening the file directly, run:

```zsh
cd "/Users/cat/Documents/GitHub/java test/Untitled/Password-Strength-Checker"
python3 -m http.server 8000
```

Then visit `http://localhost:8000/index.html`.
